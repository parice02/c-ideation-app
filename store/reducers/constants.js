const initial = {
  colors: {
    primary: "#488aff",
    secondary: "#32db64",
    danger: "#f53d3d",
    light: "#f4f4f4",
    dark: "#161616",
    maincolor: "#075e54",
  },
};

function constants(state = initial, action) {
  switch (action.type) {
    default:
      return state;
  }
}

export default constants;
